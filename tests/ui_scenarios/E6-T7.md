# E6-T7 — 上傳測試 UI（Lab 頁）

## 啟動

```
horos ui <project>          # 專案需至少有一個 completed 的訓練 run
瀏覽器開 http://localhost:5000/lab （或右上角燒杯 icon）
```

Lab 頁只做「拿自己的照片 / GIF / 影片試模型」;保留集指標與錯誤分析在
Evaluate 頁（http://localhost:5000/evaluate,準星 icon）。兩頁各自有 run 下拉。

## 測試步驟

### A. 模型選擇

1. 「Model under test」下拉列出所有 completed 的 run（id + 模型名）；
   沒有任何 completed run 時顯示「train a model first」提示
2. 切換 run 會重新載入該 run 的 Predictions 圖庫(預測屬於做出它的模型)

### B. 單張照片測試（E6-S1, E6-S3）

3. 拖一張新照片進虛線框（或點擊選檔）：
   - 立刻顯示偵測結果疊圖：類別色框 + 類別名 + 信心度標籤
   - 狀態列顯示「N detection(s) above 0.05」
4. 拉動 Confidence 滑桿：
   - 框的顯示即時增減，**無伺服器往返**（推論以 0.05 低門檻做一次，
     滑桿在前端過濾）
   - 右側顯示「N shown at ≥ t」

### B2. 全螢幕檢視器

4a. 點右欄 Predictions 圖庫的縮圖開啟全螢幕檢視器;←/→ 切換影格
4b. 關閉方式三選一:右上角叉叉、Esc、或點擊照片以外的暗色區域;
    點在照片本身、滑桿或按鈕上不會關閉

### C. 保留集指標（E6-S4 的 UI 入口,在 Evaluate 頁）

切到 Evaluate 頁(準星 icon):

5. 選 Split（test / valid）按「Run evaluation」：
   - 進度條顯示 k/N images
   - 完成後顯示四張指標卡（mAP@[.5:.95]、mAP@50、mAP@75、mAR@100）
   - 每類表格：instances、AP@50、AP@[.5:.95]、AP bar
   - 底部顯示影像數、GT 框數、評估時間
6. 報告持久化在 runs/<id>/eval/<split>.json，重新整理頁面後選同一
   run + split 仍能看到

### D. 錯誤路徑

7. 對不存在的 split 評估：同步收到明確錯誤（不會產生 job）
8. 選擇未完成（failed/stopped/running）的 run 不會出現在下拉中

## 預期結果

上傳一張新照片幾秒內看到偵測疊圖；滑桿即時改變顯示門檻；
一鍵在保留集上取得完整 COCO 指標與每類 AP

## 已知限制

- PR 曲線資料已在報告中（pr_curve_50），但尚未圖形化顯示
- 影片逐幀推論見 E6-T2 的 media 上傳流程；錯誤分析 UI 見 tests/ui_scenarios/E6-T8.md
